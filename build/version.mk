
VERSION_STRING = 0.4.9
COMMIT_STRING:=$(shell git rev-parse HEAD)

# PRODUCT_FIRMWARE_VERSION reported by default
VERSION = 11

CFLAGS += -DSYSTEM_VERSION_STRING=$(VERSION_STRING)
CFLAGS += -DGIT_COMMIT_STRING=$(COMMIT_STRING)
