syntax = "proto3";
package particle.ctrl;

import public "nanopb.proto";

message IPAddress {
    enum Protocol {
        NONE = 0;
        IPv4 = 1;
        IPv6 = 2;
    }
    Protocol protocol = 1;
    bytes address = 2 [(nanopb).max_size = 16];
}
