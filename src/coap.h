#include "tropicssl/aes.h"
#include <string.h>

void hello_from_aes_credentials(const unsigned char *credentials,
                                unsigned char *hello,
                                int &hello_size);
