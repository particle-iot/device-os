namespace CoAPMessageType {
  enum Enum {
    FUNCTION_CALL,
    VARIABLE_REQUEST,
    CHUNK,
    UPDATE_BEGIN,
    UPDATE_DONE,
    KEY_CHANGE
  };
}

/*
void hello_from_aes_credentials(const unsigned char *credentials,
                                unsigned char *hello,
                                int &hello_size);
*/
